####Week 2 intro to git

##git config --global user.name "name"
##git config --global user.email"XX@example.com"
##git config --list      (configure changes)
##exit

##Create repo

##create local copy
##mkdir ~/test-repo
##cd ~/test-repo
##git init
##git remote add origin xxx.git

##fork other user's repo
##git clone xxx.git

##git add ./-u/-A       --add all new/update tracking/ both of the previous
##git commit -m ''
##git push origin master

##when working with other people, create new branch
##git checkout -b branchname
##git branch            --see what branch you are on
##git checkout master   --switch back to the master branch type

##git pull -- update local working rpo
